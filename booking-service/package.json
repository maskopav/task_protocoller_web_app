{
  "name": "booking-service",
  "version": "1.0.0",
  "description": "Standalone, project-agnostic appointment booking/reservation service. Owns its own database and hosted booking/manage pages; integrates with any consuming app via a server-to-server admin API (API key) and a browser-facing signed-link + manage-token flow. No knowledge of task_protocoller_web_app's domain model (participants/protocols/sessions).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:init": "node src/runInit.js",
    "tenant:create": "node scripts/createTenant.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "googleapis": "^144.0.0",
    "mysql2": "^3.15.3",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "vitest": "^4.1.11"
  }
}
